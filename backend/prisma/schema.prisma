generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model qcm {
  id    Int    @id @default(autoincrement())
  email String @unique
  name  String
  note  Float
}
